import RNConfig from 'react-native-config';

const config = {
  DEFAULT_LOCALE: RNConfig.REACT_APP_DEFAULT_LOCALE,
  DEFAULT_THEME: RNConfig.REACT_APP_DEFAULT_THEME,
  BASE_URL: RNConfig.REACT_APP_BASE_URL,
};

export default config;
